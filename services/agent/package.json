{
  "name": "@tabipla/agent",
  "version": "0.1.0",
  "private": true,
  "description": "ADKエージェント(推薦/旅程/パーソナライズ/Web収集)。@google/adk + Hono。検索は @tabipla/search-core に結線予定。",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/server.ts",
    "start": "node --env-file-if-exists=.env dist/server.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "deploy": "bash scripts/deploy-agent-platform.sh",
    "deploy:cloud-run": "bash scripts/deploy.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@google/adk": "^1.2.0",
    "@google/genai": "^1.52.0",
    "@hono/node-server": "^1.19.0",
    "@tabipla/db": "workspace:*",
    "@tabipla/domain": "workspace:*",
    "@tabipla/search-core": "workspace:*",
    "hono": "^4.12.0",
    "sharp": "^0.34.3",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^7.0.0"
  }
}
