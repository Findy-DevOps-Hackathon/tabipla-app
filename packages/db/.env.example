# PostgreSQL 接続文字列（ローカル開発用の既定値）。
# 本番の認証情報はコミットしないこと。
DATABASE_URL=postgresql://tabipla:tabipla@localhost:5433/tabipla

# 管理画面シードユーザーのメールアドレス（seed 実行時・必須）
ADMIN_KOMORO_EMAIL=
ADMIN_NOTO_EMAIL=

# 管理画面シードユーザーの初期パスワード（seed 実行時・必須）
ADMIN_KOMORO_SEED_PASSWORD=
ADMIN_SEED_PASSWORD=
