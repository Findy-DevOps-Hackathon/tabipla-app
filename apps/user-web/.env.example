# 開発時: Vite が /api をプロキシする backend-api の URL
# 開発: Vite が /api を backend-api へプロキシ
VITE_API_PROXY_TARGET=http://localhost:3001

# 本番 (.env.production): VITE_API_BASE=/api のまま（firebase.json rewrite 利用）
# 直接 Cloud Run URL を指定しないこと（us-central1 等の旧 URL は 404 になる）
